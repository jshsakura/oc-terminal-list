"""LLM 사용량 — 각 호스트에 상주하는 llm-watcher 를 읽어 한 화면에 모은다.

**우리는 로그를 파싱하지 않는다.** 토큰·비용 숫자는 llm-watcher(그 안의 ccusage)가
만들고, 우리는 그걸 호스트별로 물어봐서 합칠 뿐이다. 파서와 모델 단가표를 직접 들면
새 CLI 가 나올 때마다, 가격이 바뀔 때마다 따라가야 한다.

**상주 수집기가 없다.** 백그라운드 폴러를 두지 않는다 — 대시보드가 열릴 때만 조회하고
하루 캐시한다. watcher 가 한 대도 없으면 대시보드는 이 섹션을 아예 그리지 않는다
(에이전트 기능은 옵트인).
"""
