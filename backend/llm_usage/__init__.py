"""LLM 사용량 — 에이전트가 남긴 로그를 직접 읽어 한 화면에 모은다.

**상주하는 것이 없다.** 호스트마다 컨테이너를 띄우고 포트를 열고 이미지를 갱신하는
대신, 수집기(`collect.py`) 를 그때그때 SSH stdin 으로 밀어 넣어 원격에서 한 번
실행한다 — 설치 0, 포트 0, 버전 드리프트 0. `backend/cli/itl` 과 같은 규칙이고,
보안 모델은 VNC 와 같다(새 인바운드 포트를 열지 않고 이미 있는 SSH 만 쓴다).

**폴러도 없다.** 대시보드가 열릴 때만 움직이고 하루 캐시한다. 스위치가 꺼져 있으면
로그 파일도 읽지 않는다 — 에이전트 기능은 옵트인이다.

  collect.py   로그 → 토큰(에이전트별 파서). stdlib only 단일 파일.
  runner.py    어디서 돌릴 것인가 — 로컬 import / 원격 SSH stdin.
  pricing.py   모델 → 단가. 비용을 곱하는 곳은 여기 하나뿐이다.
  aggregate.py 소스들 → 화면 한 덩어리. 순수 함수.
  service.py   수집 + 캐시 + 스위치.
"""
