fastapi==0.104.1
uvicorn[standard]==0.24.0
websockets==12.0
ptyprocess==0.7.0
python-multipart==0.0.6
python-jose[cryptography]==3.3.0
bcrypt==4.1.2
passlib==1.7.4
pyotp==2.9.0
python-dotenv==1.0.0
asyncssh==2.18.0
cryptography>=41.0.0
# 패스키 (WebAuthn / FIDO2) 로그인. RP ID / origin 은 요청 Host 에서 자동 추출.
webauthn>=2.0
# 옵셔널 — REDIS_URL 환경변수 설정 시 사용. 미설정이면 in-memory 폴백 자동.
redis>=5.0.0
