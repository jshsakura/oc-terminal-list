/**
 * 모바일 하단(퀵바 + 입력 도크) 컨트롤의 공용 치수.
 *
 * ⚠️ 이 값들이 파일마다 따로 자라면 **한 줄에 나란히 선 버튼들이 서로 다른 크기**가 된다.
 * 실제로 그랬다: 대상/히스토리는 도크에서 왔고(모서리 5px·아이콘 12) 키는 퀵바 것이라
 * (모서리 3px·아이콘 14) 같은 줄에서 어긋났고, 도크 안에서도 보내기·첨부(14)와
 * 마이크(12)가 달랐다. 각자 다른 파일에서 자란 스타일이라 코드로는 티가 안 났다.
 *
 * 여기 하나만 고치면 퀵바 키·도크 버튼·포탈로 올라간 컨트롤이 전부 따라온다.
 */
import { tokens } from './tokens';

export const MOBILE_CONTROL = {
  /** 버튼 한 변(= 텍스트 키의 최소 폭·높이). */
  size: 24,
  /** 아이콘 변. 24 상자 안에서 좌우 5px 씩 남는 크기. */
  icon: 14,
  /** 작은 아이콘은 얇으면 흐릿하다 — 살짝 굵게. */
  stroke: 2.2,
  /** 모서리 — 토큰이 정한 "버튼 기본". */
  radius: tokens.radius.sm,
  /** 그룹 구분선 높이. 버튼(24)보다 낮아야 선으로 읽힌다. */
  dividerHeight: 14,

  /**
   * 바닥 두 줄의 **위아래 여백**. 버튼 크기가 같아도 이 값이 다르면 띠 높이가 달라져
   * 한쪽 버튼이 더 커 보인다 — 실제로 퀵바 34px, 도크 32px 로 2px 어긋나 있었고,
   * 맞붙은 두 줄이라 그 2px 이 그대로 눈에 띄었다.
   * 띠 높이 = size + barPaddingY * 2.
   */
  barPaddingY: 5,

  /**
   * 바닥 영역(퀵바 + 입력 도크)의 면.
   *
   * ⚠️ 두 줄은 맞붙어 있으므로 **한 덩어리로 읽혀야 한다.** 그래서 같은 값을 쓰고,
   * 둘 사이에는 선을 긋지 않는다(선이 하나 더 있으면 영역이 쪼개져 보인다).
   *
   * ⚠️ 포커스에 따라 이 면을 밝히지 않는다. 한때 그렇게 했는데 요란하기만 했다 —
   * 신호는 입력칸 테두리 하나로 충분하다.
   */
  barBackground: 'var(--ui-mantle, #15151f)',
};

export default MOBILE_CONTROL;
